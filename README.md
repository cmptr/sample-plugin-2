# Cmptr Sample

## What it does

- Adds a ribbon icon that shows a configurable greeting.
- Adds two commands: open a greeting modal, and insert the greeting at the cursor.
- Adds a settings tab where the greeting text can be changed.
